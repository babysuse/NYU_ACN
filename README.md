# Overview

## Setup

`pipenv` is required to install the required packages.

```bash
# using apt
sudo apt install pipenv

# using brew
brew install pipenv
```

```bash
pipenv install
```

## Execution

```bash
pipenv run python main.py
```
